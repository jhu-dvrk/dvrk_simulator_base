# PSM3 instance configuration. Common defaults are in PSM.yaml.
include: PSM.yaml
robot:
  name: PSM3
  base_frame: PSM3_base
  rcm_frame: PSM3_RCM
  tool_frame: PSM3_tool
  adaptor_frame: PSM3_adaptor_link
  cartesian:
    measured_frame: PSM3_tool
